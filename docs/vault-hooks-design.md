# vault-hooks — SUPERSEDED; the design lives in the vault

> [!note] Superseded 2026-08-27 (Nelson's filing ruling + a vehicle change)
> The event→action design is no longer a new plugin and no longer lives in this repo. Reading the `tasknotes-workflows` fork showed the trigger system, workflows-as-notes, conditions, run logging, and an arbitrary-command step ALREADY EXIST there — the work is five verified gaps, not a new plugin. The living design is the vault note **`07.62 obsidian-tasknotes-workflows / Requirements & design for vault event triggers`**, which records what the fork has (with file:line references), the gaps, the MVP, and the upstream posture question.

One finding survives here because it concerns THIS repo's future: the ecosystem survey (2026-08-27) found no maintained, general-purpose event→action plugin — Shell Commands is shell-only, Templater covers create+startup, Auto Note Mover only moves, Sentinel does open/close, Commander has no events, and the two general attempts are abandoned or unproven. If the extended fork is ever considered for community release, that gap analysis is the market case, and the name `vault-hooks` was verified clean (GitHub, npm, community registry) on 2026-08-27.
