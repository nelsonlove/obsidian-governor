{
  "name": "vault-mcp-api",
  "version": "1.0.0",
  "description": "Publisher SDK for the vault-mcp external tool registry — let your Obsidian plugin publish MCP tools to Claude Code through vault-mcp.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "test": "tsc --noEmit -p tsconfig.tests.json && node --import tsx --test tests/*.test.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nelsonlove/obsidian-governor.git",
    "directory": "packages/vault-mcp-api"
  },
  "license": "MIT",
  "peerDependencies": {
    "obsidian": ">=1.4.0",
    "zod": "^3.25.28"
  },
  "dependencies": {
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "obsidian": "^1.6.6",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "zod": "^3.25.28"
  }
}
